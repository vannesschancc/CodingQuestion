import java.util.*;
public class Test {
    public static void main(String args[]) {
        String str = "abcd";
        String substr = str.substring(0, str.length());
        System.out.print(substr);
    }
}